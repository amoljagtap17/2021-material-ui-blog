import { Typography } from '@material-ui/core'
import { Title } from 'components/ui'

export const Home = () => (
  <>
    <Title titleText="Home Page" />
    <Typography variant="h6">Checkout the Blogs route :P</Typography>
  </>
)
